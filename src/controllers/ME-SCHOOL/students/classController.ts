import { catchAsync } from '../../../shared/catchAsync';
import ClassModel from './classModel';

export const CreateClassRegister = catchAsync(async (req: any, res: any) => {
  //create a register for all the classes for a particular session
});
